# Peer deps of extensions (pi framework, typebox) are bundled by pi itself —
# never resolve or install them from npm. Needed because the root package.json
# declares workspaces and npm would otherwise try to auto-install peers.
legacy-peer-deps=true
